name: openai-mcp
services:
  - name: api
    source:
      type: dockerfile
    environment:
      - name: OPENAI_API_KEY
        value: ${OPENAI_API_KEY}
    ports:
      - port: 8080
        public: true
